JavaScript
TypeScript

Node JS

2009, Ryan Dahl

interpreter (V8) + runtime (Node JS)


chrome -> V8 (C++)
Safari -> Js Core (indigo)
firefox -> Spidermonkey

Angular:
front-end JS based framework
SPA
application is built at the top of series of components
the components are clubbed and pushed into the HTML file <body> element
component => template/view + code + metadata

tool: angular cli
package: @angular/cli
>npm i @angular/cli --global


angular packages:
@angular/core
@angular/common
@angular/router
@angular/forms
@angular/platform-browser

rxjs
tslib

type		type		decorator
-------------------------------------------
component	class		@Component
Service		class		@Injectable
Directive	class		@Directive
Pipe		class		@Pipe
.........................


feature: Binding
a. data binding:
	to pass data from code to template
	1. interpolation: {{<prop-name>}}
	2. property binding:
	<element [target-attribute]="prop-name">
b. event binding:
	<element (event)="method-of-code(args)">

Zone.js => signal() -> WritableSignal<T>
			.set()
			.update()


@Ditective({
selector:'[ngFor]'
export class NgForOfDirective{
	func(templatecode){}
}
	func(templatecode){}


pms-app
	- app (folder)
		app.ts
		app.html
		app.css
		app.spec.ts
		app-routes.ts
		app-config.ts

		products (folder)
			models (folder)
				product.ts (contains Product interface/type)
			data (folder)
				products.ts (contains an array of Product objects)
			components (folder)
				product-list (folder)
					product-list.ts (ProductList) => displays image, name, price and rating of every product from the array through HTML <table> element
					product-list.html
					product-list.css
					product-list.spec.ts

			services

			pipes	




	
	





